#include<stdio.h>
int main(){
    char str[50], rev[20];
    int i,j;
    printf("Input Here: ");
    gets(str);
    rev[0]=str[0];
    rev[1]= ' ';
    while(rev[i]!='\0'){
        
    }
    return 0;
}
